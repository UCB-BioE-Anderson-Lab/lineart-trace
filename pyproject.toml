[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lineart-trace"
version = "0.2.0"
description = "A toolkit for making SVG vector art: scientific figures, diagrams and animations"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "J. Christopher Anderson" }]
keywords = ["svg", "vector graphics", "scientific figures", "diagrams",
            "vectorisation", "line art", "centreline", "bezier", "tracing"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Multimedia :: Graphics :: Graphics Conversion",
  "Topic :: Scientific/Engineering :: Image Processing",
]
dependencies = ["numpy>=1.21", "opencv-python>=4.5", "jsonschema>=4.18"]

[project.optional-dependencies]
dev = ["pytest>=7"]

[project.urls]
Source = "https://github.com/UCB-BioE-Anderson-Lab/lineart-trace"

[project.scripts]
lineart-trace = "lineart_trace.cli:main"
lineart-scene = "lineart_trace.scene.cli:main"

[tool.setuptools.packages.find]
include = ["lineart_trace*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["error::DeprecationWarning"]
