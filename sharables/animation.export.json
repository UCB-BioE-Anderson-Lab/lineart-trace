{
  "schema": "clotho.sharable/1",
  "id": "animation.export",
  "type": "function",
  "description": "Export one self-contained animated SVG, saying what it could not carry.",
  "phrases": [
    "export this as an animated SVG",
    "make a looping explainer",
    "save the animation as one file"
  ],
  "tags": [
    "animation",
    "svg",
    "smil",
    "export",
    "explainer",
    "loop"
  ],
  "noun": "animation",
  "verb": "export",
  "entry": "lineart-scene animate",
  "accepts": "scene.document",
  "requires": [
    {
      "path": "lineart_trace/scene/animate.py",
      "note": "the SMIL export"
    }
  ],
  "returns": "an animated SVG to --out; what could not be animated in it, on stderr",
  "examples": [
    {
      "input": [
        "--in",
        "tests/fixtures/scene-polymerase.json",
        "--timeline",
        "tests/fixtures/lecture.json",
        "--out",
        "/dev/null"
      ],
      "output": "[polymerase] 2.8s, 2 track(s) animated in the file, 4828 bytes"
    }
  ],
  "notes": "GENERATED from `lineart_trace.scene.cli.verb_animate` by `python3 -m lineart_trace.records`. Edit that docstring, not this file: a record written by hand is a second copy of a description that already exists beside the code, and the two drift silently."
}
