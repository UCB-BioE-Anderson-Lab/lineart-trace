{
  "schema": "clotho.sharable/1",
  "id": "scene.view.overlay",
  "type": "view",
  "description": "The payload as an annotated SVG document. A pure function; it performs no I/O.",
  "phrases": [
    "render the annotated overlay",
    "draw a scene's names boxes and anchors",
    "the inspection view of a figure",
    "show a figure's structure over the figure"
  ],
  "tags": [
    "overlay",
    "view",
    "annotation",
    "preview",
    "anchors",
    "names"
  ],
  "noun": "overlay",
  "verb": "render",
  "binds": "scene.overlay-payload",
  "requires": [
    {
      "path": "lineart_trace/scene/overlay.py",
      "note": "the render function; payload in, markup out"
    }
  ],
  "returns": "an annotated SVG document for one payload; performs no I/O",
  "notes": "GENERATED from `lineart_trace.scene.overlay.render` by `python3 -m lineart_trace.records`. A view performs no I/O: it reads no store, calls no capability and measures nothing, so it can be rendered from a fixture and checked without the toolkit running."
}
